# Copy to secrets.yaml in this directory and enter real values locally.
# Never commit secrets.yaml.
wifi_ssid: "YOUR_WIFI_NAME"
wifi_password: "YOUR_WIFI_PASSWORD"
e_paper_infoscreen_api_key: "YOUR_EXISTING_ESPHOME_API_ENCRYPTION_KEY"
e_paper_infoscreen_ota_password: "YOUR_EXISTING_OTA_PASSWORD"
# Only needed if the fallback access point is enabled:
e_paper_infoscreen_fallback_password: "YOUR_FALLBACK_AP_PASSWORD"
