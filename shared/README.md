# Shared components and assets

Reserve this directory for ESPHome packages, fonts, or images used by more than
one display. The first display remains self-contained; no shared configuration
has been extracted yet. Document consumers and relative include paths when
adding a shared resource, and validate all affected displays when changing it.
